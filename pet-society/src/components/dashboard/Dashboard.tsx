import React from "react";

import { Card, CardContent, CardHeader, Container } from "@mui/material";

const Dashboard = () => {
  return (
    <>
      <Container>
        <Card>
          <CardHeader title={"testing"} />
          <CardContent>hard to say</CardContent>
        </Card>
      </Container>
    </>
  );
};

export default Dashboard;
